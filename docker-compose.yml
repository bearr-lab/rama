version: '3.8'

services:
  # ── Next.js 16 Production Web Application ──
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rama-web
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - rama-mesh

  # ── Isolated Storybook Component Documentation Server ──
  storybook:
    build:
      context: .
      dockerfile: Dockerfile.storybook
    container_name: rama-storybook
    restart: unless-stopped
    ports:
      - "6006:6006"
    networks:
      - rama-mesh

  # ── High-Speed Redis Event & Response Cache ──
  redis:
    image: redis:7-alpine
    container_name: rama-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - rama-mesh

networks:
  rama-mesh:
    driver: bridge

volumes:
  redis-data:
